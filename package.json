{
  "name": "prisma-banking",
  "version": "0.13.3",
  "description": "PRISMA Banking — plataforma profissional de formação bancária",
  "main": "electron-main.js",
  "author": "PRISMA Educacional",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "electron .",
    "dist": "npm run dist:server",
    "dist:server": "npm run prepare:backend && npm run verify && npm run preflight:build && electron-builder --win nsis --config build/electron-builder.server.json",
    "dist:server:portable": "npm run prepare:backend && npm run verify && npm run preflight:build && electron-builder --win portable --config build/electron-builder.server.json",
    "dist:client": "node scripts/verify-client.js && electron-builder --win nsis --config build/electron-builder.client.json",
    "dist:all": "npm run dist:server && npm run dist:server:portable && npm run dist:client",
    "dist:portable": "npm run dist:server:portable",
    "prepare:backend": "npm ci --prefix backend --omit=dev",
    "pre-dist": "npm run prepare:backend && npm run verify && npm run preflight:build",
    "pre-dist:portable": "npm run prepare:backend && npm run verify && npm run preflight:build",
    "verify": "node scripts/verify.js",
    "verify:client": "node scripts/verify-client.js",
    "preflight:build": "node scripts/preflight-build.js"
  },
  "devDependencies": {
    "electron": "^37.2.6",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "ao.prisma.educacional.banking",
    "productName": "PRISMA Banking",
    "asar": true,
    "asarUnpack": [
      "backend/**/*"
    ],
    "files": [
      "electron-main.js",
      "electron/**/*",
      "backend/**/*",
      "frontend/**/*"
    ],
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "PRISMA-Banking-${version}-${arch}.${ext}",
      "icon": "frontend/assets/prisma-banking.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  }
}