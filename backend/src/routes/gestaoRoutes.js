const express=require('express');const c=require('../controllers/gestaoController');const { exigirPermissao } = require('../middleware/permissoes');
const r=express.Router();r.get('/utilizadores',exigirPermissao('USUARIOS_GERIR'),c.listarUtilizadores);r.patch('/utilizadores/:id',exigirPermissao('USUARIOS_GERIR'),c.alterarUtilizador);r.get('/auditoria',exigirPermissao('AUDITORIA_VISUALIZAR'),c.auditoria);r.get('/relatorios/resumo',exigirPermissao('RELATORIOS_VISUALIZAR'),c.relatorio);r.get('/reconciliacao',exigirPermissao('CAIXA_RECONCILIAR'),c.reconciliacao);module.exports=r;
